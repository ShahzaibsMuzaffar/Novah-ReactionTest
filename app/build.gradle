plugins {
    id 'com.android.application'
}

android {
    namespace 'com.novah.reactiontest'
    compileSdk 34

    defaultConfig {
        applicationId "com.novah.reactiontest"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
